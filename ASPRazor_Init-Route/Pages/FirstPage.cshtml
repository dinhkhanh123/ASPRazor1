@page "/trang-dau-tien/{solanlap:int?}"
@model FirstPageModel
@{

    ViewData["Title"] = "Trang chu";
}

<h1 style="color: red;">@Model.title</h1>

<h2>@ViewData["mydata"]</h2>

<p>Thoi gian hien tai la <strong>@System.DateTime.Now</strong></p>

@{
    int a = 2;
    int b = 3;
    var solanlapstring = this.Request.RouteValues["solanlap"] ?? "0";

    int somuc = Int32.Parse(solanlapstring.ToString());
    <p>Can bac hai cua @a la @Math.Sqrt(a)</p>
}

<p>Tong cua @a va @b la @(a + b)</p>
<p>So lan lap la @somuc</p>
<ol>
    @for (int i = 1; i <= somuc; i++)
    {
        <li>So muc @i</li>
    }
</ol>

<p>@Model.welcome("Tran Van A")</p>
<a asp-page="SecondPage">Trang thu 2</a>